# Configurazione del bot Telegram
# SECURITY: Non condividere mai il tuo token reale in repository pubblici!
TELEGRAM_BOT_TOKEN=12345:your_bot_token_here

# Modalità di funzionamento (webhook o polling)
# SECURITY: In produzione, utilizzare sempre 'webhook' per maggiore sicurezza
MODE=webhook

# URL del webhook (per la modalità webhook)
# SECURITY: Utilizzare sempre HTTPS per proteggere le comunicazioni
WEBHOOK_URL=https://your-domain.com

# Porta per il webhook (default: 443)
PORT=443

# Percorsi dei certificati SSL
SSL_CERT_PATH=certs/your_ssl_cert.pem
SSL_KEY_PATH=certs/your_ssl_key.pem

# API Key per Have I Been Pwned 
HIBP_API_KEY=your_hibp_api_key_here

# Configurazione per rate limiting 
RATE_LIMIT_WINDOW=60  # Finestra temporale in secondi
RATE_LIMIT_MAX_CALLS=5  # Numero massimo di richieste nella finestra